diff --git a/apps/desktop/src/app/settings/config-field.preview-wiring.test.tsx b/apps/desktop/src/app/settings/config-field.preview-wiring.test.tsx
new file mode 100644
index 0000000000..fd538bf64e
--- /dev/null
+++ b/apps/desktop/src/app/settings/config-field.preview-wiring.test.tsx
@@ -0,0 +1,55 @@
+import { cleanup, render, screen } from '@testing-library/react'
+import { afterEach, describe, expect, it, vi } from 'vitest'
+
+// Keep the row shell out of the way: this test is about which rows get the audition button.
+vi.mock('./primitives', () => ({
+  ListRow: ({ action, title }: { action?: unknown; title?: string }) => (
+    <div>
+      <span>{title}</span>
+      {action as never}
+    </div>
+  )
+}))
+
+const directTtsConfig = vi.fn().mockResolvedValue(null)
+
+vi.mock('@/lib/voice-client-direct', () => ({
+  clearVoiceClientConfigCache: vi.fn(),
+  directTtsConfig: () => directTtsConfig()
+}))
+
+vi.mock('@/lib/voice-playback', () => ({ playSpeechText: vi.fn(), stopVoicePlayback: vi.fn() }))
+
+import { ConfigField } from './config-field'
+
+const renderField = (schemaKey: string) =>
+  render(
+    <ConfigField
+      onChange={() => {}}
+      schema={{ description: '', type: 'string' } as never}
+      schemaKey={schemaKey}
+      value="aura-2-thalia-en"
+    />
+  )
+
+afterEach(() => cleanup())
+
+describe('ConfigField audition wiring', () => {
+  it('puts a Preview button on the OpenRouter voice row', () => {
+    renderField('tts.openrouter.voice')
+    expect(screen.getByRole('button').textContent).toContain('Preview')
+  })
+
+  it('puts one on the speed row too, so a rate can be heard', () => {
+    renderField('tts.openrouter.speed')
+    expect(screen.getByRole('button').textContent).toContain('Preview')
+  })
+
+  it('leaves every other row alone', () => {
+    for (const key of ['tts.openrouter.model', 'tts.openai.voice', 'stt.openrouter.model', 'tts.provider']) {
+      cleanup()
+      renderField(key)
+      expect(screen.queryByRole('button')).toBeNull()
+    }
+  })
+})
diff --git a/apps/desktop/src/app/settings/config-field.tsx b/apps/desktop/src/app/settings/config-field.tsx
index 43d713ef6a..9d73922779 100644
--- a/apps/desktop/src/app/settings/config-field.tsx
+++ b/apps/desktop/src/app/settings/config-field.tsx
@@ -15,6 +15,12 @@ import { FallbackModelsField } from './fallback-models-field'
 import { fieldCopyForSchemaKey } from './field-copy'
 import { ListRow } from './primitives'
 import { SearchableSelect } from './searchable-select'
+import { VoicePreviewButton } from './voice-preview-button'
+
+// Rows whose value is audible: the local OpenRouter patch puts an audition button beside them, so a
+// voice or a playback rate can be checked without leaving Settings (it previews the CURRENT
+// model + voice + speed, including values the autosave has just written).
+const AUDITION_KEYS = new Set(['tts.openrouter.voice', 'tts.openrouter.speed'])
 
 /**
  * One generic config row: label + description resolved from the i18n field
@@ -78,7 +84,22 @@ export function ConfigField({
   // point at one setting (`[data-tour="field-model"]`) without hunting through
   // the section for an nth-child path. See lib/tour.
   const row = (action: ReactNode, wide = false) => (
-    <ListRow action={action} data-tour={`field-${schemaKey}`} description={descriptionNode} title={label} wide={wide} />
+    <ListRow
+      action={
+        AUDITION_KEYS.has(schemaKey) ? (
+          <div className="flex items-center justify-end gap-2">
+            {action}
+            <VoicePreviewButton />
+          </div>
+        ) : (
+          action
+        )
+      }
+      data-tour={`field-${schemaKey}`}
+      description={descriptionNode}
+      title={label}
+      wide={wide}
+    />
   )
 
   // `fallback_providers` is a list of {provider, model} objects; the generic
diff --git a/apps/desktop/src/app/settings/constants.ts b/apps/desktop/src/app/settings/constants.ts
index 0a0c04b03c..db3d74d61d 100644
--- a/apps/desktop/src/app/settings/constants.ts
+++ b/apps/desktop/src/app/settings/constants.ts
@@ -361,6 +361,149 @@ export const ENUM_OPTIONS: Record<string, string[]> = {
   ],
   'stt.openai.model': ['whisper-1', 'gpt-4o-mini-transcribe', 'gpt-4o-transcribe', 'gpt-transcribe'],
   'stt.mistral.model': ['voxtral-mini-latest', 'voxtral-mini-2602'],
+  // OpenRouter transcription catalog — vendor-prefixed slugs, all 20 probed live 2026-09-14.
+  // Free-input (FREE_INPUT_KEYS): the live catalog moves faster than this list.
+  'stt.openrouter.model': [
+    'openai/whisper-large-v3',
+    'openai/whisper-large-v3-turbo',
+    'openai/whisper-1',
+    'openai/gpt-4o-transcribe',
+    'openai/gpt-4o-mini-transcribe',
+    'openai/gpt-transcribe',
+    'mistralai/voxtral-mini-transcribe',
+    'google/chirp-3',
+    'deepgram/nova-3',
+    'x-ai/grok-stt-1.0',
+    'qwen/qwen3-asr-flash-2026-02-10',
+    'microsoft/mai-transcribe-2',
+    'nvidia/parakeet-tdt-0.6b-v3',
+    'fish-audio/transcribe-1',
+    'meta/muse-voice-transcribe-1.0',
+    'mistralai/voxtral-small-24b-2507-stt',
+    'qwen/qwen3-asr-1.7b',
+    'qwen/qwen3-asr-0.6b',
+    'microsoft/mai-transcribe-1.5',
+    'nvidia/nemotron-3.5-asr-streaming-multilingual-0.6b'
+  ],
+  // OpenRouter speech catalog — vendor-prefixed slugs, all 18 probed live 2026-09-14.
+  'tts.openrouter.model': [
+
+    'canopylabs/orpheus-3b-0.1-ft',
+    'deepgram/aura-2',
+    'deepgram/flux-tts:free',
+    'fish-audio/s1',
+    'fish-audio/s2-pro',
+    'fish-audio/s2.1-pro',
+    'fish-audio/s2.1-pro-free:free',
+    'google/gemini-3.1-flash-tts-preview',
+    'hexgrad/kokoro-82m',
+    'microsoft/mai-voice-2',
+    'microsoft/mai-voice-2-flash',
+    'minimax/speech-2.8-hd',
+    'minimax/speech-2.8-turbo',
+    'mistralai/voxtral-mini-tts-2603',
+    'qwen/qwen-audio-3.0-tts-flash',
+    'qwen/qwen-audio-3.0-tts-plus',
+    'sesame/csm-1b',
+    'x-ai/grok-voice-tts-1.0'
+
+  ],
+  // Voices are MODEL-SPECIFIC and the endpoint rejects a mismatched pair, so this baseline is the
+  // default model's published set; enumOptionsFor() swaps in the selected model's set.
+  'tts.openrouter.voice': [
+
+    'aura-2-thalia-en',
+    'aura-2-agathe-fr',
+    'aura-2-agustina-es',
+    'aura-2-alvaro-es',
+    'aura-2-ama-ja',
+    'aura-2-amalthea-en',
+    'aura-2-andromeda-en',
+    'aura-2-antonia-es',
+    'aura-2-apollo-en',
+    'aura-2-aquila-es',
+    'aura-2-arcas-en',
+    'aura-2-aries-en',
+    'aura-2-asteria-en',
+    'aura-2-athena-en',
+    'aura-2-atlas-en',
+    'aura-2-aurelia-de',
+    'aura-2-aurora-en',
+    'aura-2-beatrix-nl',
+    'aura-2-callista-en',
+    'aura-2-carina-es',
+    'aura-2-celeste-es',
+    'aura-2-cesare-it',
+    'aura-2-cinzia-it',
+    'aura-2-cora-en',
+    'aura-2-cordelia-en',
+    'aura-2-cornelia-nl',
+    'aura-2-daphne-nl',
+    'aura-2-delia-en',
+    'aura-2-demetra-it',
+    'aura-2-diana-es',
+    'aura-2-dionisio-it',
+    'aura-2-draco-en',
+    'aura-2-ebisu-ja',
+    'aura-2-elara-de',
+    'aura-2-electra-en',
+    'aura-2-elio-it',
+    'aura-2-estrella-es',
+    'aura-2-fabian-de',
+    'aura-2-flavio-it',
+    'aura-2-fujin-ja',
+    'aura-2-gloria-es',
+    'aura-2-harmonia-en',
+    'aura-2-hector-fr',
+    'aura-2-helena-en',
+    'aura-2-hera-en',
+    'aura-2-hermes-en',
+    'aura-2-hestia-nl',
+    'aura-2-hyperion-en',
+    'aura-2-iris-en',
+    'aura-2-izanami-ja',
+    'aura-2-janus-en',
+    'aura-2-javier-es',
+    'aura-2-julius-de',
+    'aura-2-juno-en',
+    'aura-2-jupiter-en',
+    'aura-2-kara-de',
+    'aura-2-lara-de',
+    'aura-2-lars-nl',
+    'aura-2-leda-nl',
+    'aura-2-livia-it',
+    'aura-2-luciano-es',
+    'aura-2-luna-en',
+    'aura-2-maia-it',
+    'aura-2-mars-en',
+    'aura-2-melia-it',
+    'aura-2-minerva-en',
+    'aura-2-neptune-en',
+    'aura-2-nestor-es',
+    'aura-2-odysseus-en',
+    'aura-2-olivia-es',
+    'aura-2-ophelia-en',
+    'aura-2-orion-en',
+    'aura-2-orpheus-en',
+    'aura-2-pandora-en',
+    'aura-2-phoebe-en',
+    'aura-2-pluto-en',
+    'aura-2-rhea-nl',
+    'aura-2-roman-nl',
+    'aura-2-sander-nl',
+    'aura-2-saturn-en',
+    'aura-2-selena-es',
+    'aura-2-selene-en',
+    'aura-2-silvia-es',
+    'aura-2-sirio-es',
+    'aura-2-theia-en',
+    'aura-2-uzume-ja',
+    'aura-2-valerio-es',
+    'aura-2-vesta-en',
+    'aura-2-viktoria-de',
+    'aura-2-zeus-en'
+
+  ],
   'tts.openai.model': ['gpt-4o-mini-tts', 'tts-1', 'tts-1-hd'],
   'tts.elevenlabs.model_id': ['eleven_multilingual_v2', 'eleven_turbo_v2_5', 'eleven_flash_v2_5'],
   // NeuTTS local inference device.
@@ -373,6 +516,424 @@ export const ENUM_OPTIONS: Record<string, string[]> = {
 // cloned voices, xAI custom voices, Edge's 400+ catalog) and ship new model
 // names faster than this list updates. The ENUM_OPTIONS above become
 // suggestions rather than a gate for these keys.
+// OpenRouter speech voices, keyed by model (`supported_voices` from the models API — there is no
+// voices endpoint). Used to narrow the voice field to what the selected model accepts.
+export const OPENROUTER_TTS_VOICES: Record<string, string[]> = {
+  // 7 voices
+  'canopylabs/orpheus-3b-0.1-ft': [
+    'tara',
+    'leah',
+    'jess',
+    'leo',
+    'dan',
+    'mia',
+    'zac',
+  ],
+  // 90 voices
+  'deepgram/aura-2': [
+    'aura-2-thalia-en',
+    'aura-2-agathe-fr',
+    'aura-2-agustina-es',
+    'aura-2-alvaro-es',
+    'aura-2-ama-ja',
+    'aura-2-amalthea-en',
+    'aura-2-andromeda-en',
+    'aura-2-antonia-es',
+    'aura-2-apollo-en',
+    'aura-2-aquila-es',
+    'aura-2-arcas-en',
+    'aura-2-aries-en',
+    'aura-2-asteria-en',
+    'aura-2-athena-en',
+    'aura-2-atlas-en',
+    'aura-2-aurelia-de',
+    'aura-2-aurora-en',
+    'aura-2-beatrix-nl',
+    'aura-2-callista-en',
+    'aura-2-carina-es',
+    'aura-2-celeste-es',
+    'aura-2-cesare-it',
+    'aura-2-cinzia-it',
+    'aura-2-cora-en',
+    'aura-2-cordelia-en',
+    'aura-2-cornelia-nl',
+    'aura-2-daphne-nl',
+    'aura-2-delia-en',
+    'aura-2-demetra-it',
+    'aura-2-diana-es',
+    'aura-2-dionisio-it',
+    'aura-2-draco-en',
+    'aura-2-ebisu-ja',
+    'aura-2-elara-de',
+    'aura-2-electra-en',
+    'aura-2-elio-it',
+    'aura-2-estrella-es',
+    'aura-2-fabian-de',
+    'aura-2-flavio-it',
+    'aura-2-fujin-ja',
+    'aura-2-gloria-es',
+    'aura-2-harmonia-en',
+    'aura-2-hector-fr',
+    'aura-2-helena-en',
+    'aura-2-hera-en',
+    'aura-2-hermes-en',
+    'aura-2-hestia-nl',
+    'aura-2-hyperion-en',
+    'aura-2-iris-en',
+    'aura-2-izanami-ja',
+    'aura-2-janus-en',
+    'aura-2-javier-es',
+    'aura-2-julius-de',
+    'aura-2-juno-en',
+    'aura-2-jupiter-en',
+    'aura-2-kara-de',
+    'aura-2-lara-de',
+    'aura-2-lars-nl',
+    'aura-2-leda-nl',
+    'aura-2-livia-it',
+    'aura-2-luciano-es',
+    'aura-2-luna-en',
+    'aura-2-maia-it',
+    'aura-2-mars-en',
+    'aura-2-melia-it',
+    'aura-2-minerva-en',
+    'aura-2-neptune-en',
+    'aura-2-nestor-es',
+    'aura-2-odysseus-en',
+    'aura-2-olivia-es',
+    'aura-2-ophelia-en',
+    'aura-2-orion-en',
+    'aura-2-orpheus-en',
+    'aura-2-pandora-en',
+    'aura-2-phoebe-en',
+    'aura-2-pluto-en',
+    'aura-2-rhea-nl',
+    'aura-2-roman-nl',
+    'aura-2-sander-nl',
+    'aura-2-saturn-en',
+    'aura-2-selena-es',
+    'aura-2-selene-en',
+    'aura-2-silvia-es',
+    'aura-2-sirio-es',
+    'aura-2-theia-en',
+    'aura-2-uzume-ja',
+    'aura-2-valerio-es',
+    'aura-2-vesta-en',
+    'aura-2-viktoria-de',
+    'aura-2-zeus-en',
+  ],
+  // 36 voices
+  'deepgram/flux-tts:free': [
+    'flux-alexis-en',
+    'flux-bree-en',
+    'flux-brittany-en',
+    'flux-brooke-en',
+    'flux-bruce-en',
+    'flux-cliff-en',
+    'flux-cole-en',
+    'flux-colin-en',
+    'flux-conor-en',
+    'flux-donovan-en',
+    'flux-drew-en',
+    'flux-elise-en',
+    'flux-gemma-en',
+    'flux-haley-en',
+    'flux-hannah-en',
+    'flux-heather-en',
+    'flux-jack-en',
+    'flux-kai-en',
+    'flux-kelsey-en',
+    'flux-kit-en',
+    'flux-maeve-en',
+    'flux-marcelo-en',
+    'flux-marcus-en',
+    'flux-meena-en',
+    'flux-meghan-en',
+    'flux-miles-en',
+    'flux-naveen-en',
+    'flux-paige-en',
+    'flux-priya-en',
+    'flux-rufus-en',
+    'flux-sean-en',
+    'flux-sharon-en',
+    'flux-sienna-en',
+    'flux-tanner-en',
+    'flux-wade-en',
+    'flux-wes-en',
+  ],
+  // no list published — the provider default voice speaks for this model
+  'fish-audio/s1': [],
+  // no list published — the provider default voice speaks for this model
+  'fish-audio/s2-pro': [],
+  // no list published — the provider default voice speaks for this model
+  'fish-audio/s2.1-pro': [],
+  // no list published — the provider default voice speaks for this model
+  'fish-audio/s2.1-pro-free:free': [],
+  // 30 voices
+  'google/gemini-3.1-flash-tts-preview': [
+    'Zephyr',
+    'Puck',
+    'Charon',
+    'Kore',
+    'Fenrir',
+    'Leda',
+    'Orus',
+    'Aoede',
+    'Callirrhoe',
+    'Autonoe',
+    'Enceladus',
+    'Iapetus',
+    'Umbriel',
+    'Algieba',
+    'Despina',
+    'Erinome',
+    'Algenib',
+    'Rasalgethi',
+    'Laomedeia',
+    'Achernar',
+    'Alnilam',
+    'Schedar',
+    'Gacrux',
+    'Pulcherrima',
+    'Achird',
+    'Zubenelgenubi',
+    'Vindemiatrix',
+    'Sadachbia',
+    'Sadaltager',
+    'Sulafat',
+  ],
+  // 54 voices
+  'hexgrad/kokoro-82m': [
+    'af_alloy',
+    'af_aoede',
+    'af_bella',
+    'af_heart',
+    'af_jessica',
+    'af_kore',
+    'af_nicole',
+    'af_nova',
+    'af_river',
+    'af_sarah',
+    'af_sky',
+    'am_adam',
+    'am_echo',
+    'am_eric',
+    'am_fenrir',
+    'am_liam',
+    'am_michael',
+    'am_onyx',
+    'am_puck',
+    'am_santa',
+    'bf_alice',
+    'bf_emma',
+    'bf_isabella',
+    'bf_lily',
+    'bm_daniel',
+    'bm_fable',
+    'bm_george',
+    'bm_lewis',
+    'ef_dora',
+    'em_alex',
+    'em_santa',
+    'ff_siwis',
+    'hf_alpha',
+    'hf_beta',
+    'hm_omega',
+    'hm_psi',
+    'if_sara',
+    'im_nicola',
+    'jf_alpha',
+    'jf_gongitsune',
+    'jf_nezumi',
+    'jf_tebukuro',
+    'jm_kumo',
+    'pf_dora',
+    'pm_alex',
+    'pm_santa',
+    'zf_xiaobei',
+    'zf_xiaoni',
+    'zf_xiaoxiao',
+    'zf_xiaoyi',
+    'zm_yunjian',
+    'zm_yunxi',
+    'zm_yunxia',
+    'zm_yunyang',
+  ],
+  // 4 voices
+  'microsoft/mai-voice-2': [
+    'en-US-Harper:MAI-Voice-2',
+    'es-MX-Valeria:MAI-Voice-2',
+    'fr-FR-Soleil:MAI-Voice-2',
+    'de-DE-Klaus:MAI-Voice-2',
+  ],
+  // 4 voices
+  'microsoft/mai-voice-2-flash': [
+    'en-US-Harper:MAI-Voice-2',
+    'es-MX-Valeria:MAI-Voice-2',
+    'fr-FR-Soleil:MAI-Voice-2',
+    'de-DE-Klaus:MAI-Voice-2',
+  ],
+  // 45 voices
+  'minimax/speech-2.8-hd': [
+    'English_expressive_narrator',
+    'English_radiant_girl',
+    'English_magnetic_voiced_man',
+    'English_compelling_lady1',
+    'English_Aussie_Bloke',
+    'English_captivating_female1',
+    'English_Upbeat_Woman',
+    'English_Trustworth_Man',
+    'English_CalmWoman',
+    'English_UpsetGirl',
+    'English_Gentle-voiced_man',
+    'English_Whispering_girl',
+    'English_Diligent_Man',
+    'English_Graceful_Lady',
+    'English_ReservedYoungMan',
+    'English_PlayfulGirl',
+    'English_ManWithDeepVoice',
+    'English_MaturePartner',
+    'English_FriendlyPerson',
+    'English_MatureBoss',
+    'English_Debator',
+    'English_LovelyGirl',
+    'English_Steadymentor',
+    'English_Deep-VoicedGentleman',
+    'English_Wiselady',
+    'English_CaptivatingStoryteller',
+    'English_DecentYoungMan',
+    'English_SentimentalLady',
+    'English_ImposingManner',
+    'English_SadTeen',
+    'English_PassionateWarrior',
+    'English_WiseScholar',
+    'English_Soft-spokenGirl',
+    'English_SereneWoman',
+    'English_ConfidentWoman',
+    'English_PatientMan',
+    'English_Comedian',
+    'English_BossyLeader',
+    'English_Strong-WilledBoy',
+    'English_StressedLady',
+    'English_AssertiveQueen',
+    'English_AnimeCharacter',
+    'English_Jovialman',
+    'English_WhimsicalGirl',
+    'English_Kind-heartedGirl',
+  ],
+  // 45 voices
+  'minimax/speech-2.8-turbo': [
+    'English_expressive_narrator',
+    'English_radiant_girl',
+    'English_magnetic_voiced_man',
+    'English_compelling_lady1',
+    'English_Aussie_Bloke',
+    'English_captivating_female1',
+    'English_Upbeat_Woman',
+    'English_Trustworth_Man',
+    'English_CalmWoman',
+    'English_UpsetGirl',
+    'English_Gentle-voiced_man',
+    'English_Whispering_girl',
+    'English_Diligent_Man',
+    'English_Graceful_Lady',
+    'English_ReservedYoungMan',
+    'English_PlayfulGirl',
+    'English_ManWithDeepVoice',
+    'English_MaturePartner',
+    'English_FriendlyPerson',
+    'English_MatureBoss',
+    'English_Debator',
+    'English_LovelyGirl',
+    'English_Steadymentor',
+    'English_Deep-VoicedGentleman',
+    'English_Wiselady',
+    'English_CaptivatingStoryteller',
+    'English_DecentYoungMan',
+    'English_SentimentalLady',
+    'English_ImposingManner',
+    'English_SadTeen',
+    'English_PassionateWarrior',
+    'English_WiseScholar',
+    'English_Soft-spokenGirl',
+    'English_SereneWoman',
+    'English_ConfidentWoman',
+    'English_PatientMan',
+    'English_Comedian',
+    'English_BossyLeader',
+    'English_Strong-WilledBoy',
+    'English_StressedLady',
+    'English_AssertiveQueen',
+    'English_AnimeCharacter',
+    'English_Jovialman',
+    'English_WhimsicalGirl',
+    'English_Kind-heartedGirl',
+  ],
+  // 30 voices
+  'mistralai/voxtral-mini-tts-2603': [
+    'en_paul_sad',
+    'en_paul_neutral',
+    'en_paul_happy',
+    'en_paul_frustrated',
+    'en_paul_excited',
+    'en_paul_confident',
+    'en_paul_cheerful',
+    'en_paul_angry',
+    'gb_oliver_neutral',
+    'gb_oliver_sad',
+    'gb_oliver_excited',
+    'gb_oliver_curious',
+    'gb_oliver_confident',
+    'gb_oliver_cheerful',
+    'gb_oliver_angry',
+    'gb_jane_sarcasm',
+    'gb_jane_confused',
+    'gb_jane_shameful',
+    'gb_jane_sad',
+    'gb_jane_neutral',
+    'gb_jane_jealousy',
+    'gb_jane_frustrated',
+    'gb_jane_curious',
+    'gb_jane_confident',
+    'fr_marie_sad',
+    'fr_marie_neutral',
+    'fr_marie_happy',
+    'fr_marie_excited',
+    'fr_marie_curious',
+    'fr_marie_angry',
+  ],
+  // 2 voices
+  'qwen/qwen-audio-3.0-tts-flash': [
+    'loongjohn',
+    'longanhuan_v3.6',
+  ],
+  // 2 voices
+  'qwen/qwen-audio-3.0-tts-plus': [
+    'longanlingxin',
+    'longanlufeng',
+  ],
+  // 7 voices
+  'sesame/csm-1b': [
+    'conversational_a',
+    'conversational_b',
+    'read_speech_a',
+    'read_speech_b',
+    'read_speech_c',
+    'read_speech_d',
+    'none',
+  ],
+  // 5 voices
+  'x-ai/grok-voice-tts-1.0': [
+    'eve',
+    'ara',
+    'rex',
+    'sal',
+    'leo',
+  ],
+}
+
+export const OPENROUTER_TTS_DEFAULT_MODEL = 'deepgram/aura-2'
+
 export const FREE_INPUT_KEYS = new Set([
   'tts.edge.voice',
   'voice.gpt_live.voice',
@@ -391,12 +952,20 @@ export const FREE_INPUT_KEYS = new Set([
   'tts.kittentts.voice',
   'tts.piper.voice',
   'tts.deepinfra.model',
-  'tts.deepinfra.voice'
+  'tts.deepinfra.voice',
+  'stt.openrouter.model',
+  'tts.openrouter.model',
+  'tts.openrouter.voice'
 ])
 
 export const FIELD_LABELS: Record<string, string> = defineFieldCopy({
   model: 'Default Model',
   modelContextLength: 'Context Window',
+  // Provider-scoped voice rows: the fallback label is prettyName(lastSegment) => a bare "Model".
+  'stt.openrouter.model': 'OpenRouter Model',
+  'tts.openrouter.model': 'OpenRouter Voice Model',
+  'tts.openrouter.voice': 'OpenRouter Voice',
+  'tts.openrouter.speed': 'OpenRouter Playback Speed',
   fallbackProviders: 'Fallback Models',
   toolsets: 'Enabled Toolsets',
   timezone: 'Timezone',
@@ -572,6 +1141,10 @@ export const FIELD_LABELS: Record<string, string> = defineFieldCopy({
 export const FIELD_DESCRIPTIONS: Record<string, string> = defineFieldCopy({
   model: 'Used for new chats unless you pick a different model in the composer.',
   modelContextLength: "Leave at 0 to use the selected model's detected context window.",
+  'stt.openrouter.model': 'Vendor-prefixed OpenRouter slug, e.g. openai/whisper-large-v3. Audited live: meta/muse-voice-transcribe-1.0 needs 16 or 24 kHz WAV.',
+  'tts.openrouter.model': 'Vendor-prefixed OpenRouter speech slug, e.g. deepgram/aura-2. Hexgrad Kokoro emits only the first sentence; Gemini TTS needs pcm.',
+  'tts.openrouter.voice': 'Voice for the selected model — the list follows the model, since voices are model-specific.',
+  'tts.openrouter.speed': 'Playback speed. 0.7 = slower, 1.0 = normal, 1.5 = faster. Applied natively where the model supports it, otherwise by a local pitch-preserving time-stretch.',
   fallbackProviders: 'Backup provider:model entries to try if the default model fails.',
   display: {
     personality: 'Default assistant style for new sessions.',
@@ -747,6 +1320,9 @@ export const SECTIONS: DesktopConfigSection[] = [
       'stt.enabled',
       'stt.echo_transcripts',
       'stt.provider',
+      // OpenRouter is a command provider (stt.providers.openrouter), not a built-in, so its model
+      // row is keyed on the selected provider name and gated by voiceFieldVisible().
+      'stt.openrouter.model',
       'voice.auto_tts',
       'tts.edge.voice',
       'tts.openai.model',
@@ -773,6 +1349,12 @@ export const SECTIONS: DesktopConfigSection[] = [
       'tts.piper.voice',
       'tts.deepinfra.model',
       'tts.deepinfra.voice',
+      // OpenRouter is a command provider (tts.providers.openrouter), not a built-in: the model
+      // decides which voices are valid (the API rejects a mismatched pair), and helpers.ts narrows
+      // the voice suggestions to the selected model.
+      'tts.openrouter.model',
+      'tts.openrouter.voice',
+      'tts.openrouter.speed',
       'stt.local.model',
       'stt.local.language',
       'stt.openai.model',
diff --git a/apps/desktop/src/app/settings/helpers.ts b/apps/desktop/src/app/settings/helpers.ts
index b51162f854..cc4d4d8313 100644
--- a/apps/desktop/src/app/settings/helpers.ts
+++ b/apps/desktop/src/app/settings/helpers.ts
@@ -1,7 +1,14 @@
 import { asText, normalize } from '@/lib/text'
 import type { ConfigFieldSchema, HermesConfigRecord, ToolsetInfo } from '@/types/hermes'
 
-import { BUILTIN_PERSONALITIES, ENUM_OPTIONS, PROVIDER_GROUPS, SECTIONS } from './constants'
+import {
+  BUILTIN_PERSONALITIES,
+  ENUM_OPTIONS,
+  OPENROUTER_TTS_DEFAULT_MODEL,
+  OPENROUTER_TTS_VOICES,
+  PROVIDER_GROUPS,
+  SECTIONS
+} from './constants'
 
 // Canonical implementations live in @/lib/text; re-exported here so the many
 // settings/capabilities call sites keep their import path.
@@ -376,6 +383,20 @@ export function enumOptionsFor(
     }
   }
 
+  // OpenRouter voices are per model too: offering an Aura-2 voice against Kokoro is a 400. The
+  // table is the models API's `supported_voices`, so the list follows the selected model.
+  if (!dynamicOptions && opts && key === 'tts.openrouter.voice') {
+    const model = asText(getNested(config, 'tts.openrouter.model')) || OPENROUTER_TTS_DEFAULT_MODEL
+    const voices = OPENROUTER_TTS_VOICES[model]
+
+    if (voices) {
+      // An empty set is meaningful: those models publish no voices and speak with the provider's
+      // default, so offering another model's voices would only invite a 400. The field stays
+      // free-input (FREE_INPUT_KEYS), and the current value is re-appended below.
+      opts = [...voices]
+    }
+  }
+
   if (!opts) {
     return undefined
   }
diff --git a/apps/desktop/src/app/settings/voice-preview-button.test.tsx b/apps/desktop/src/app/settings/voice-preview-button.test.tsx
new file mode 100644
index 0000000000..0b258bbcba
--- /dev/null
+++ b/apps/desktop/src/app/settings/voice-preview-button.test.tsx
@@ -0,0 +1,97 @@
+import { cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react'
+import { afterEach, describe, expect, it, vi } from 'vitest'
+
+import { setVoicePlaybackState } from '@/store/voice-playback'
+import type { VoicePlaybackSource } from '@/store/voice-playback'
+
+const directTtsConfig = vi.fn()
+const clearVoiceClientConfigCache = vi.fn()
+const playSpeechText = vi.fn()
+const stopVoicePlayback = vi.fn()
+
+vi.mock('@/lib/voice-client-direct', () => ({
+  clearVoiceClientConfigCache: () => clearVoiceClientConfigCache(),
+  directTtsConfig: () => directTtsConfig()
+}))
+
+vi.mock('@/lib/voice-playback', () => ({
+  playSpeechText: (...args: unknown[]) => playSpeechText(...args),
+  stopVoicePlayback: () => stopVoicePlayback()
+}))
+
+import { VoicePreviewButton } from './voice-preview-button'
+
+function idle() {
+  setVoicePlaybackState({ audioElement: null, messageId: null, sequence: 0, source: null, status: 'idle' })
+}
+
+afterEach(() => {
+  cleanup()
+  idle()
+  vi.clearAllMocks()
+})
+
+describe('VoicePreviewButton', () => {
+  it('drops the cached config, then speaks the resolved voice by name', async () => {
+    directTtsConfig.mockResolvedValue({ voice: 'af_heart' })
+    playSpeechText.mockResolvedValue(true)
+
+    render(<VoicePreviewButton />)
+    fireEvent.click(screen.getByRole('button'))
+
+    await waitFor(() => expect(playSpeechText).toHaveBeenCalledTimes(1))
+
+    // the 60s config cache must not answer with the previous model/voice/speed
+    expect(clearVoiceClientConfigCache).toHaveBeenCalledTimes(1)
+    expect(clearVoiceClientConfigCache.mock.invocationCallOrder[0])
+      .toBeLessThan(directTtsConfig.mock.invocationCallOrder[0])
+
+    const [text, options] = playSpeechText.mock.calls[0] as [string, { messageId: null; source: VoicePlaybackSource }]
+    expect(text).toBe('af_heart. This is a sample of my voice.')
+    expect(options.source).toBe('read-aloud')
+    expect(options.messageId).toBeNull()
+  })
+
+  it('falls back to a generic sample when no direct config is available', async () => {
+    directTtsConfig.mockResolvedValue(null)
+    playSpeechText.mockResolvedValue(true)
+
+    render(<VoicePreviewButton />)
+    fireEvent.click(screen.getByRole('button'))
+
+    await waitFor(() => expect(playSpeechText).toHaveBeenCalledTimes(1))
+    expect(playSpeechText.mock.calls[0][0]).toBe('This is a sample of my voice.')
+  })
+
+  it('reports when nothing could play the sample', async () => {
+    directTtsConfig.mockResolvedValue(null)
+    playSpeechText.mockResolvedValue(false)
+
+    render(<VoicePreviewButton />)
+    fireEvent.click(screen.getByRole('button'))
+
+    expect(await screen.findByText(/No speech path available/)).toBeTruthy()
+  })
+
+  it('surfaces a provider error instead of failing silently', async () => {
+    directTtsConfig.mockResolvedValue({ voice: 'af_heart' })
+    playSpeechText.mockRejectedValue(new Error('OpenRouter TTS error (HTTP 400): nope'))
+
+    render(<VoicePreviewButton />)
+    fireEvent.click(screen.getByRole('button'))
+
+    expect(await screen.findByText(/HTTP 400/)).toBeTruthy()
+  })
+
+  it('offers Stop while playback is active and stops on click', () => {
+    setVoicePlaybackState({ audioElement: null, messageId: null, sequence: 1, source: 'read-aloud', status: 'speaking' })
+
+    render(<VoicePreviewButton />)
+    const button = screen.getByRole('button')
+    expect(button.textContent).toContain('Stop')
+
+    fireEvent.click(button)
+    expect(stopVoicePlayback).toHaveBeenCalledTimes(1)
+    expect(playSpeechText).not.toHaveBeenCalled()
+  })
+})
diff --git a/apps/desktop/src/app/settings/voice-preview-button.tsx b/apps/desktop/src/app/settings/voice-preview-button.tsx
new file mode 100644
index 0000000000..ae2c5c7173
--- /dev/null
+++ b/apps/desktop/src/app/settings/voice-preview-button.tsx
@@ -0,0 +1,72 @@
+import { useStore } from '@nanostores/react'
+import { useState } from 'react'
+
+import { Button } from '@/components/ui/button'
+import { clearVoiceClientConfigCache, directTtsConfig } from '@/lib/voice-client-direct'
+import { playSpeechText, stopVoicePlayback } from '@/lib/voice-playback'
+import { $voicePlayback } from '@/store/voice-playback'
+
+/**
+ * Audition button for the OpenRouter voice rows (part of the local GUI patch).
+ *
+ * Plays a short sample using whatever is configured *right now* — model, voice and speed — through
+ * the app's own playback ladder: client-direct synthesis when the profile has client-callable TTS
+ * credentials, otherwise the gateway relay. The relay is the interesting path here: it runs the
+ * OpenRouter command provider server-side, so the measured speed handling applies (native parameter
+ * inside the model's range, local pitch-preserving stretch outside it).
+ *
+ * The voice-config cache is dropped first. It is TTL'd at 60 s, and a preview should reflect the
+ * model/voice/speed you just picked, not the values cached a minute ago.
+ */
+const SAMPLE = 'This is a sample of my voice.'
+
+export function VoicePreviewButton() {
+  const playback = useStore($voicePlayback)
+  const [busy, setBusy] = useState(false)
+  const [error, setError] = useState('')
+  const playing = playback.status !== 'idle'
+
+  const toggle = async () => {
+    if (playing) {
+      stopVoicePlayback()
+      return
+    }
+
+    setError('')
+    setBusy(true)
+
+    try {
+      clearVoiceClientConfigCache()
+      // Naming the voice in the sample makes a montage of them self-labelling; the resolved config
+      // is the only place the effective voice id is known (the field may hold an empty or a
+      // model-specific value).
+      const config = await directTtsConfig().catch(() => null)
+      const voice = config?.voice?.trim()
+      const text = voice ? `${voice}. ${SAMPLE}` : SAMPLE
+      const played = await playSpeechText(text, { messageId: null, source: 'read-aloud' })
+
+      if (!played) {
+        setError('No speech path available — check the provider credentials.')
+      }
+    } catch (err) {
+      setError(err instanceof Error ? err.message.slice(0, 120) : 'Preview failed')
+    } finally {
+      setBusy(false)
+    }
+  }
+
+  return (
+    <div className="flex flex-col items-end gap-1">
+      <Button
+        disabled={busy}
+        onClick={() => void toggle()}
+        size="sm"
+        title="Play a sample with the selected model, voice and speed"
+        variant="secondary"
+      >
+        {playing ? 'Stop' : busy ? 'Preparing…' : 'Preview'}
+      </Button>
+      {error ? <span className="max-w-56 text-right text-xs text-muted-foreground">{error}</span> : null}
+    </div>
+  )
+}
